package org.csu.ecmusic.controller;

import org.csu.ecmusic.model.Account.Sigon;
import org.csu.ecmusic.service.AccountService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

/**
 *
 * 允许跨域访问*/
@RestController
@CrossOrigin
public class AccountController {
//    @Autowired
////    private AccountService accountService;
////
////    @PostMapping("Account/Sigon/{username}")
////    public Sigon sigon(@PathVariable("username") String username){
////        return  accountService.getSigonByUsername(username);
////    }

    /**
     * 完成用户模块功能*/

}
